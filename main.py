import numpy as np
from funciones import*
from variables import*
#Hay que hacer algo con los tableros
disparadno = True
while disparando:
    fila = int(input("Elije fila entre 0-9: "))
    columna = int(input("Elije fila entre 0-9: "))
